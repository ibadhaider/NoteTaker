This app was built using https://mindmup.github.io/bootstrap-wysiwyg/ as the rich text editor for the user to write notes in,
also uses express and Node.js to create the server. Has an auto-save feeature which saves the current selected note every minute.
